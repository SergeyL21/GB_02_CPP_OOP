// ------------------------------------------------------------------------------------
// ЗАДАНИЕ 1: Реализовать шаблон класса Pair1, который позволяет пользователю
// передавать данные одного типа парами.
{
  cout << " --- TASK 1 --- " << endl;
  namespace n = task_1;
  n::Pair<int> p1{6, 9};
  const n::Pair<double> p2(3.4, 7.8);
  cout << "Pair: " << p1.first() << ' ' << p1.second() << endl;
  cout << "Pair: " << p2.first() << ' ' << p2.second() << endl;
}
